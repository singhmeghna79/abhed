## What changes, and why

<!-- One paragraph. Say what a user or operator can now do, or what stops going wrong. -->

## How it is verified

<!-- The test that fails without this change, or the command you ran and its output. -->

## Checklist

- [ ] `make check` passes locally
- [ ] New behaviour has a test that fails without it
- [ ] Documentation in `docs/` updated where behaviour changed
- [ ] Commits are signed off (`git commit -s`)
